# Stub Dockerfile for banking-api (Spring Boot)
# Replace with actual build from github.com/acme-bank-inc/banking-api
FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY . .
EXPOSE 8080
CMD ["java", "-jar", "app.jar"]
